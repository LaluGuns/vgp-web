node_modules/
.next/
out/
build/
.env
.env.local
.DS_Store
Thumbs.db
npm-debug.log
yarn-debug.log
yarn-error.log
.vercel
*.tsbuildinfo
tsconfig.tsbuildinfo

# Heavy blog source working material (raw AI images, ~109MB).
# Not served by the app and not referenced in code — kept local only,
# never committed/deployed. Archive to Supabase/CloudFront if needed.
content/**/*.png
content/**/*.jpg
content/**/*.jpeg
content/**/*.webp

# Private executed legal evidence — never commit, publish, or deploy.
# These carry a handwritten signature / PII. Only unsigned public templates
# belong in the repo (see flowstate/docs/legal/).
legal-intake/
*ORIGINAL_SIGNED_EVIDENCE*
*RIGHTS_AND_ID_APPROVAL*
*_EXECUTED*
*SIGNATURE_TRANSPARENT*
*SIGNATURE_PREVIEW*

# Editor/AI tool config
.claude/
.env*
.headroom-venv/
.headroom-cache/
.headroom-proxy.*
